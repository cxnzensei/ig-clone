import { atom } from "recoil";

export const modalPostState = atom({
	key: "modalPostState",
	default: false,
});

export const modalProfilePhotoState = atom({
	key: "modalProfilePhotoState",
	default: false,
});
